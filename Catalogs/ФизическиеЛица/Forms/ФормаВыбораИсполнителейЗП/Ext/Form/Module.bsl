
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Инцидент") Тогда
		ЗадачаПроекта = Параметры.Инцидент.Владелец.Владелец;
	КонецЕсли;
	Параметры.Свойство("ДатаИсполнения", 	ДатаИсполнения);
	
	мФизическиеЛица = УП_ПланыРаботПоПроектам.ИсполнителиЗадачиПроекта( ЗадачаПроекта, ДатаИсполнения );
	
	Список.Параметры.УстановитьЗначениеПараметра("ФизическиеЛица", мФизическиеЛица );
КонецПроцедуры
