
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.ДобавитьСтроку(ТекстОбАвторе());
	ТекстовыйДокумент.ТолькоПросмотр = Истина;
	ТекстовыйДокумент.Показать("Об авторе подсистемы");
	
КонецПроцедуры


&НаСервере
Функция ТекстОбАвторе()
	
	Ответ = Обработки.ТелеграмОбновлениеВерсии.ПолучитьМакет("ОбАвторе").ПолучитьТекст();
	Возврат Ответ;
	
КонецФункции
